---
title: Secrets, rotation, health checks, and scaling
description: Manage versioned secrets, external secret providers, deployment health, autoscaling, and database replication settings.
---

## Versioned secrets

Upstand keeps secret values separate from ordinary resource configuration and supports versioned secret workflows. A version can be listed, restored, or used as the basis for a rotation. Secret-management routes require the active organization, the relevant capability, and step-up verification when 2FA is enabled.

The model does not receive raw secret values through UpGal configuration inspection. Treat the dashboard's reveal/rotation workflows as privileged operations and record changes in the audit log.

## External secret providers

Secret provider integrations currently support Vault, AWS Secrets Manager, and 1Password. A provider stores encrypted configuration and can be enabled or disabled. Syncing reads values from the selected provider and merges them into the requested secret scope; a disabled or missing provider is rejected.

For AWS Secrets Manager, the provider configuration must supply a region, access key ID, secret access key, and secret path. Keep those credentials narrowly scoped to the required secret paths.

## Rotation schedules

Create a rotation schedule for a secret scope and selected keys. The scheduler claims due work so it is not processed concurrently, rotates only the requested keys, and records the resulting version/change. Rotation can trigger the normal deployment/update workflow needed for the new value to take effect.

Plan for consumers that cache credentials: rotating a value changes the secret version, but the running workload may need a redeploy or restart to load it.

## Health checks

Application and Compose resources can define a health check command with interval, timeout, retry count, and start period. Use a command that is available in the image and checks the service's real readiness rather than merely returning the process version.

Health checks work with the selected deployment strategy and automatic rollback settings. A health-check failure is evidence for a deployment decision, not a guarantee that the underlying application is defective; inspect logs and metrics before rolling back.

## Autoscaling

Autoscaling is an advanced setting for application resources. It reconciles Swarm replicas using one or more targets: CPU percentage, memory percentage, requests per second, or a custom metric. Configure minimum and maximum replicas, scale-up/down step sizes, and a cooldown period. Enabling autoscaling requires at least one target metric.

Scaling is bounded by the configured replica limits and cooldown. The runtime skips Compose and database resources. Keep enough host capacity for the maximum replica count and pair scaling with a health check and monitoring so a metric-driven increase does not hide an application-level failure.

## Deployment strategies and replication

Advanced deployment strategies include rolling, canary, blue-green, and progressive modes, with bake time, optional steps, and automatic rollback configuration. Database resources separately expose managed replication settings with a replica count, automatic failover flag, and replication user; validate the selected database image and storage topology before enabling them.
