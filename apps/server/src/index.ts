/**
 * Server package entrypoint.
 *
 * The HTTP application and its lifecycle are assembled in `server.ts`; this
 * stable entrypoint is intentionally small for Bun, tsdown, and consumers.
 */
export { default } from "./server";
